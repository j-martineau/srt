#' @encoding UTF-8
#' @title Calculate Achievement Levels from a Cut Score Data Frame
#' @param x A student-level data frame.
#' @param cuts A data frame of cut scores resulting from a call to \code{\link{ingest_cuts}}.
#' @param curr.ss Character scalar name of the variable in `x` containing current-year scaled scores.
#' @return `x` with an achievement level (`ach.lev`) variable attached.
calc_ALs_from_cuts <- function(x, cuts, curr.ss) {

}
